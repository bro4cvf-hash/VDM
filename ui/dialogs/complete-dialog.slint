import { Icon } from "../components/icon.slint";
import { CaptionBtn } from "../components/buttons.slint";
import { Palette } from "../theme.slint";

// ════════════════════════════════════════════════
// Download Complete Dialog (IDM-style Modern Glass)
// ════════════════════════════════════════════════
export component CompleteDialog inherits Window {
    width: 490px;
    height: 315px;
    no-frame: true;
    background: transparent;
    title: "Download complete";
    default-font-family: "Segoe UI Variable Display, Segoe UI, Inter, -apple-system, sans-serif";

    in-out property <string> task-id: "";
    in-out property <string> filename: "";
    in-out property <string> size: "";
    in-out property <string> total-bytes-text: "";
    in-out property <string> url: "";
    in-out property <string> location: "";
    in-out property <image> file-icon;
    in-out property <bool> has-sys-icon: false;
    in-out property <string> file-type: "file";
    in-out property <bool> dont-show-again: false;

    callback open-file(string);
    callback open-with(string);
    callback open-folder(string);
    callback start-drag(string);
    callback dont-show-changed(bool);
    callback closed();
    callback drag-window();

    Rectangle {
        width: 100%;
        height: 100%;
        border-radius: 12px;
        background: Palette.modal-bg;
        border-width: 1px;
        border-color: Palette.border-color;
        clip: true;

        VerticalLayout {
            padding: 0;
            spacing: 0;

            // ── Seamless Blended Header ──
            Rectangle {
                height: 36px;
                background: transparent;

                TouchArea {
                    pointer-event(pe) => {
                        if (pe.button == PointerEventButton.left && pe.kind == PointerEventKind.down) {
                            root.drag-window();
                        }
                    }
                    moved => {
                        if (self.pressed) {
                            root.drag-window();
                        }
                    }
                }

                HorizontalLayout {
                    padding-left: 14px;
                    padding-right: 8px;
                    alignment: space-between;

                    HorizontalLayout {
                        spacing: 8px;
                        alignment: center;

                        Rectangle {
                            width: 16px;
                            height: 16px;
                            y: (parent.height - self.height) / 2;

                            Image {
                                source: @image-url("../icons/vdm.svg");
                                width: 16px;
                                height: 16px;
                            }
                        }

                        Text {
                            text: "Download complete";
                            font-size: 12.5px;
                            font-weight: 600;
                            color: Palette.text-primary;
                            vertical-alignment: center;
                        }
                    }

                    CaptionBtn {
                        glyph: "✕";
                        danger: true;
                        clicked => { root.closed(); }
                    }
                }
            }

            // ── Body ──
            VerticalLayout {
                padding-left: 16px;
                padding-right: 16px;
                padding-top: 10px;
                padding-bottom: 14px;
                spacing: 11px;

                // Hero Complete Info Header
                HorizontalLayout {
                    spacing: 12px;
                    alignment: center;

                    Rectangle {
                        width: 40px;
                        height: 40px;
                        border-radius: 8px;
                        background: Palette.card-secondary-bg;
                        border-width: 1px;
                        border-color: Palette.border-subtle;

                        if root.has-sys-icon: Image {
                            source: root.file-icon;
                            width: 26px;
                            height: 26px;
                            x: (parent.width - self.width) / 2;
                            y: (parent.height - self.height) / 2;
                        }

                        if !root.has-sys-icon: Icon {
                            source: @image-url("../icons/tick.svg");
                            width: 20px;
                            height: 20px;
                            x: (parent.width - self.width) / 2;
                            y: (parent.height - self.height) / 2;
                            tint: Palette.success;
                        }
                    }

                    VerticalLayout {
                        horizontal-stretch: 1;
                        alignment: center;
                        spacing: 2px;

                        Text {
                            text: "Download complete";
                            font-size: 13.5px;
                            font-weight: 700;
                            color: Palette.text-primary;
                        }

                        Text {
                            text: "Downloaded " + root.size + (root.total-bytes-text != "" ? " (" + root.total-bytes-text + ")" : "");
                            font-size: 11.5px;
                            color: Palette.text-secondary;
                        }
                    }
                }

                // Address field
                VerticalLayout {
                    spacing: 3px;
                    Text { text: "Address"; font-size: 10px; font-weight: 700; letter-spacing: 0.6px; color: Palette.text-secondary; }
                    Rectangle {
                        height: 28px;
                        border-radius: 5px;
                        background: Palette.input-bg;
                        border-width: 1px;
                        border-color: Palette.border-color;
                        clip: true;
                        HorizontalLayout {
                            padding-left: 8px;
                            padding-right: 8px;
                            Text {
                                text: root.url;
                                font-size: 11px;
                                color: Palette.accent-light;
                                overflow: elide;
                                vertical-alignment: center;
                            }
                        }
                    }
                }

                // The file saved as field
                VerticalLayout {
                    spacing: 3px;
                    Text { text: "The file saved as"; font-size: 10px; font-weight: 700; letter-spacing: 0.6px; color: Palette.text-secondary; }
                    Rectangle {
                        height: 28px;
                        border-radius: 5px;
                        background: Palette.input-bg;
                        border-width: 1px;
                        border-color: Palette.border-color;
                        clip: true;
                        HorizontalLayout {
                            padding-left: 8px;
                            padding-right: 8px;
                            Text {
                                text: root.location;
                                font-size: 11px;
                                color: Palette.text-primary;
                                overflow: elide;
                                vertical-alignment: center;
                            }
                        }
                    }
                }

                // Action Buttons: Open, Open with..., Open folder, Close
                HorizontalLayout {
                    spacing: 8px;
                    alignment: start;

                    // Open
                    Rectangle {
                        height: 32px;
                        min-width: 78px;
                        border-radius: 6px;
                        background: op-t.pressed ? Palette.accent-pressed :
                                    op-t.has-hover ? Palette.accent-hover : Palette.accent;
                        drop-shadow-blur: op-t.has-hover ? 10px : 2px;
                        drop-shadow-color: Palette.accent-tint-bg;
                        animate background { duration: 120ms; }

                        op-t := TouchArea {
                            clicked => {
                                root.open-file(root.task-id);
                            }
                        }

                        Text {
                            text: "Open";
                            font-size: 12px;
                            font-weight: 600;
                            color: #FFFFFF;
                            x: (parent.width - self.width) / 2;
                            y: (parent.height - self.height) / 2;
                        }
                    }

                    // Open with...
                    Rectangle {
                        height: 32px;
                        min-width: 94px;
                        border-radius: 6px;
                        background: opw-t.pressed ? Palette.hover-bg :
                                    opw-t.has-hover ? Palette.hover-bg : Palette.card-secondary-bg;
                        border-width: 1px;
                        border-color: Palette.border-subtle;
                        animate background { duration: 120ms; }

                        opw-t := TouchArea {
                            clicked => {
                                root.open-with(root.task-id);
                            }
                        }

                        Text {
                            text: "Open with…";
                            font-size: 12px;
                            font-weight: 500;
                            color: Palette.text-primary;
                            x: (parent.width - self.width) / 2;
                            y: (parent.height - self.height) / 2;
                        }
                    }

                    // Open folder
                    Rectangle {
                        height: 32px;
                        min-width: 94px;
                        border-radius: 6px;
                        background: opf-t.pressed ? Palette.hover-bg :
                                    opf-t.has-hover ? Palette.hover-bg : Palette.card-secondary-bg;
                        border-width: 1px;
                        border-color: Palette.border-subtle;
                        animate background { duration: 120ms; }

                        opf-t := TouchArea {
                            clicked => {
                                root.open-folder(root.task-id);
                            }
                        }

                        Text {
                            text: "Open folder";
                            font-size: 12px;
                            font-weight: 500;
                            color: Palette.text-primary;
                            x: (parent.width - self.width) / 2;
                            y: (parent.height - self.height) / 2;
                        }
                    }

                    // Close
                    Rectangle {
                        height: 32px;
                        min-width: 72px;
                        border-radius: 6px;
                        background: cl-t.pressed ? Palette.hover-bg :
                                    cl-t.has-hover ? Palette.hover-bg : Palette.card-secondary-bg;
                        border-width: 1px;
                        border-color: Palette.border-subtle;
                        animate background { duration: 120ms; }

                        cl-t := TouchArea {
                            clicked => { root.closed(); }
                        }

                        Text {
                            text: "Close";
                            font-size: 12px;
                            font-weight: 500;
                            color: Palette.text-primary;
                            x: (parent.width - self.width) / 2;
                            y: (parent.height - self.height) / 2;
                        }
                    }
                }

                // Bottom bar: Checkbox & Interactive Accent Drag Handle
                HorizontalLayout {
                    spacing: 10px;
                    alignment: space-between;

                    // Don't show again Checkbox
                    HorizontalLayout {
                        spacing: 8px;
                        alignment: start;

                        Rectangle {
                            width: 15px;
                            height: 15px;
                            y: (parent.height - self.height) / 2;
                            border-radius: 4px;
                            background: root.dont-show-again ? Palette.accent : Palette.card-secondary-bg;
                            border-width: 1px;
                            border-color: root.dont-show-again ? Palette.accent : Palette.border-color;
                            animate background { duration: 120ms; }
                            TouchArea { clicked => { root.dont-show-again = !root.dont-show-again; root.dont-show-changed(root.dont-show-again); } }
                            if root.dont-show-again: Icon {
                                source: @image-url("../icons/tick.svg");
                                width: 9px;
                                height: 9px;
                                x: (parent.width - self.width) / 2;
                                y: (parent.height - self.height) / 2;
                                tint: #FFFFFF;
                            }
                        }
                        Text {
                            text: "Don't show this dialog again";
                            font-size: 11.5px;
                            color: Palette.text-secondary;
                            vertical-alignment: center;
                            TouchArea { clicked => { root.dont-show-again = !root.dont-show-again; root.dont-show-changed(root.dont-show-again); } }
                        }
                    }

                    // Interactive Accent Drag File Button Pill with Tooltip
                    Rectangle {
                        height: 28px;
                        border-radius: 6px;
                        background: drag-t.pressed ? Palette.accent-pressed :
                                    drag-t.has-hover ? Palette.accent-hover : Palette.accent-tint-bg;
                        border-width: 1px;
                        border-color: drag-t.has-hover ? Palette.accent : Palette.border-subtle;
                        drop-shadow-blur: drag-t.has-hover ? 10px : 0px;
                        drop-shadow-color: Palette.accent-tint-bg;
                        animate background { duration: 120ms; }
                        animate border-color { duration: 120ms; }

                        HorizontalLayout {
                            padding-left: 8px;
                            padding-right: 8px;
                            spacing: 5px;
                            alignment: center;

                            Rectangle {
                                width: 12px;
                                height: 12px;
                                y: (parent.height - self.height) / 2;
                                Icon {
                                    source: @image-url("../icons/folder.svg");
                                    width: 12px;
                                    height: 12px;
                                    x: (parent.width - self.width) / 2;
                                    y: (parent.height - self.height) / 2;
                                    tint: drag-t.has-hover ? #FFFFFF : Palette.accent-light;
                                }
                            }

                            Text {
                                text: "Drag / Reveal";
                                font-size: 11px;
                                font-weight: 600;
                                color: drag-t.has-hover ? #FFFFFF : Palette.accent-light;
                                vertical-alignment: center;
                            }
                        }

                        drag-t := TouchArea {
                            mouse-cursor: grab;
                            clicked => {
                                root.start-drag(root.task-id);
                            }
                        }

                        // Hover tooltip
                        if drag-t.has-hover: Rectangle {
                            x: -70px;
                            y: -28px;
                            height: 22px;
                            border-radius: 4px;
                            background: Palette.surface-bg;
                            border-width: 1px;
                            border-color: Palette.border-color;
                            drop-shadow-blur: 10px;
                            drop-shadow-color: #000000dd;
                            z: 100;

                            HorizontalLayout {
                                padding-left: 6px;
                                padding-right: 6px;
                                alignment: center;
                                Text {
                                    text: "Drag or click to reveal in Explorer";
                                    font-size: 10px;
                                    font-weight: 500;
                                    color: Palette.text-primary;
                                    vertical-alignment: center;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
